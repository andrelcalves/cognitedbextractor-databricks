version: 1
type: local

logger:
  console:
    level: DEBUG

  file:
    level: DEBUG
    path: "./dbextractor.log"
    retention: 7

cognite:
  host: "https://az-phx-001.cognitedata.com"
  project: "bdx-dev"
  timeout: 3000

  idp-authentication:
    authority: "https://login.microsoftonline.com/"
    tenant: ""
    client-id: ""
    secret: ""
    scopes:
      - "https://az-phx-001.cognitedata.com/.default"

  connection:
    disable-ssl: true
  
  data-set:
    external-id: 'ds_databricks_everest_dat'

  extraction-pipeline:
    external-id: 'ep_databricks_everest_dbExtractor'

databases:
  - type: "odbc"
    name: "db-databricks-raw"
    connection-string: 
      Driver={Databricks ODBC Driver};
      Host=;
      Port=443;
      HTTPPath=;
      SSL=1;
      ThriftTransport=2;
      AuthMech=3;
      UID=token;
      PWD= 

queries:
# WORKCENTER TABLES

# TC25 - Work Center Formulas
  - name: "extract-everest-TC25"
    database: "db-databricks-raw"
    query: >
      SELECT CONCAT(MANDT, '_', IDENT) AS TC25_PK, *
      FROM hub_dev.g_external.v_cognite_tc25_everest
      WHERE coalesce(
              try_to_timestamp(DATETIMESTAMP, 'yyyy-MM-dd HH:mm:ss.SSSSSS'),
              try_to_timestamp(DATETIMESTAMP, 'yyyy-MM-dd HH:mm:ss')
            ) >= current_timestamp() - INTERVAL 1 YEAR
    destination:
      type: "raw"
      database: "db_databricks_everest_raw"
      table: "tb_TC25"

    primary-key: "{TC25_PK}"

# CRHD - Work Center Header
  - name: "extract-everest-CRHD"
    database: "db-databricks-raw"
    query: >
      SELECT CONCAT(MANDT, '_', OBJTY, '_', OBJID, '_', BEGDA, '_', ENDDA) AS CRHD_PK, *
      FROM hub_dev.g_external.v_cognite_crhd_everest
      WHERE coalesce(
              try_to_timestamp(DATETIMESTAMP, 'yyyy-MM-dd HH:mm:ss.SSSSSS'),
              try_to_timestamp(DATETIMESTAMP, 'yyyy-MM-dd HH:mm:ss')
            ) >= current_timestamp() - INTERVAL 1 YEAR
    destination:
      type: "raw"
      database: "db_databricks_everest_raw"
      table: "tb_CRHD"

    primary-key: "{CRHD_PK}"

# CRTX - Text for the Work Center or Production Resource/Tool
  - name: "extract-everest-CRTX"
    database: "db-databricks-raw"
    query: >
      SELECT CONCAT(MANDT, '_', OBJTY, '_', OBJID, '_', SPRAS) AS CRTX_PK, *
      FROM hub_dev.g_external.v_cognite_crtx_everest
      WHERE coalesce(
              try_to_timestamp(DATETIMESTAMP, 'yyyy-MM-dd HH:mm:ss.SSSSSS'),
              try_to_timestamp(DATETIMESTAMP, 'yyyy-MM-dd HH:mm:ss')
            ) >= current_timestamp() - INTERVAL 1 YEAR
    destination:
      type: "raw"
      database: "db_databricks_everest_raw"
      table: "tb_CRTX"

    primary-key: "{CRTX_PK}"

# CRCO - Assignment of Work Center to Cost Center
  - name: "extract-everest-CRCO"
    database: "db-databricks-raw"
    query: >
      SELECT CONCAT(MANDT, '_', OBJTY, '_', OBJID, '_', LASET, '_', ENDDA, '_', LANUM) AS CRCO_PK, *
      FROM hub_dev.g_external.v_cognite_crco_everest
      WHERE coalesce(
              try_to_timestamp(DATETIMESTAMP, 'yyyy-MM-dd HH:mm:ss.SSSSSS'),
              try_to_timestamp(DATETIMESTAMP, 'yyyy-MM-dd HH:mm:ss')
            ) >= current_timestamp() - INTERVAL 1 YEAR
    destination:
      type: "raw"
      database: "db_databricks_everest_raw"
      table: "tb_CRCO"

    primary-key: "{CRCO_PK}"

# CRCA - Work Center Capacity Allocation
  - name: "extract-everest-CRCA"
    database: "db-databricks-raw"
    query: >
      SELECT CONCAT(MANDT, '_', OBJTY, '_', OBJID, '_', CANUM) AS CRCA_PK, *
      FROM hub_dev.g_external.v_cognite_crca_everest
      WHERE coalesce(
              try_to_timestamp(DATETIMESTAMP, 'yyyy-MM-dd HH:mm:ss.SSSSSS'),
              try_to_timestamp(DATETIMESTAMP, 'yyyy-MM-dd HH:mm:ss')
            ) >= current_timestamp() - INTERVAL 1 YEAR
    destination:
      type: "raw"
      database: "db_databricks_everest_raw"
      table: "tb_CRCA"

    primary-key: "{CRCA_PK}"

# WORKCENTER END

# COSTCENTER TABLES

# CSKS - Cost Center Master Data
  - name: "extract-everest-CSKS"
    database: "db-databricks-raw"
    query: >
      SELECT CONCAT(MANDT, '_', KOKRS, '_', KOSTL, '_', DATBI) AS CSKS_PK, *
      FROM hub_dev.g_external.v_cognite_csks_everest
      WHERE coalesce(
              try_to_timestamp(DATETIMESTAMP, 'yyyy-MM-dd HH:mm:ss.SSSSSS'),
              try_to_timestamp(DATETIMESTAMP, 'yyyy-MM-dd HH:mm:ss')
            ) >= current_timestamp() - INTERVAL 1 YEAR
    destination:
      type: "raw"
      database: "db_databricks_everest_raw"
      table: "tb_CSKS"

    primary-key: "{CSKS_PK}"

# CSKT - Cost Center Texts
  - name: "extract-everest-CSKT"
    database: "db-databricks-raw"
    query: >
      SELECT CONCAT(MANDT, '_', SPRAS, '_', KOKRS, '_', KOSTL, '_', DATBI) AS CSKT_PK, *
      FROM hub_dev.g_external.v_cognite_cskt_everest
      WHERE coalesce(
              try_to_timestamp(DATETIMESTAMP, 'yyyy-MM-dd HH:mm:ss.SSSSSS'),
              try_to_timestamp(DATETIMESTAMP, 'yyyy-MM-dd HH:mm:ss')
            ) >= current_timestamp() - INTERVAL 1 YEAR
    destination:
      type: "raw"
      database: "db_databricks_everest_raw"
      table: "tb_CSKT"

    primary-key: "{CSKT_PK}"

# END COSTCENTER

# FUNCTIONAL LOCATION TABLES

# IFLOT - Functional Location Master Data
  - name: "extract-everest-IFLOT"
    database: "db-databricks-raw"
    query: >
      SELECT CONCAT(MANDT, '_' , TPLNR) AS IFLOS_PK, *
      FROM hub_dev.g_external.v_cognite_iflot_everest
      WHERE coalesce(
              try_to_timestamp(DATETIMESTAMP, 'yyyy-MM-dd HH:mm:ss.SSSSSS'),
              try_to_timestamp(DATETIMESTAMP, 'yyyy-MM-dd HH:mm:ss')
            ) >= current_timestamp() - INTERVAL 1 YEAR
    destination:
      type: "raw"
      database: "db_databricks_everest_raw"
      table: "tb_IFLOT"

    primary-key: "{IFLOT_PK}"

# IFLOTX - Functional Location Short Texts
  - name: "extract-everest-IFLOTX"
    database: "db-databricks-raw"
    query: >
      SELECT CONCAT(MANDT, '_', TPLNR, '_', SPRAS) AS IFLOTX_PK, *
      FROM hub_dev.g_external.v_cognite_iflotx_everest
      WHERE coalesce(
              try_to_timestamp(DATETIMESTAMP, 'yyyy-MM-dd HH:mm:ss.SSSSSS'),
              try_to_timestamp(DATETIMESTAMP, 'yyyy-MM-dd HH:mm:ss')
            ) >= current_timestamp() - INTERVAL 1 YEAR
    destination:
      type: "raw"
      database: "db_databricks_everest_raw"
      table: "tb_IFLOTX"

    primary-key: "{IFLOTX_PK}"

# IFLOS - Functional Location Alternative Labels
  - name: "extract-everest-IFLOS"
    database: "db-databricks-raw"
    query: >
      SELECT CONCAT(MANDT, '_' , TPLNR, '_', ALKEY , '_', VERSN) AS IFLOS_PK, *
      FROM hub_dev.g_external.v_cognite_iflos_everest
      WHERE coalesce(
              try_to_timestamp(DATETIMESTAMP, 'yyyy-MM-dd HH:mm:ss.SSSSSS'),
              try_to_timestamp(DATETIMESTAMP, 'yyyy-MM-dd HH:mm:ss')
            ) >= current_timestamp() - INTERVAL 1 YEAR
    destination:
      type: "raw"
      database: "db_databricks_everest_raw"
      table: "tb_IFLOS"

    primary-key: "{IFLOS_PK}"

# ILOA - PM Object Location and Account Assignment
  - name: "extract-everest-ILOA"
    database: "db-databricks-raw"
    query: >
      SELECT *
      FROM hub_dev.g_external.v_cognite_iloa_everest
      WHERE coalesce(
              try_to_timestamp(DATETIMESTAMP, 'yyyy-MM-dd HH:mm:ss.SSSSSS'),
              try_to_timestamp(DATETIMESTAMP, 'yyyy-MM-dd HH:mm:ss')
            ) >= current_timestamp() - INTERVAL 1 YEAR
    destination:
      type: "raw"
      database: "db_databricks_everest_raw"
      table: "tb_ILOA"
    primary-key: "{ILOAN}"

# END FUNCTIONAL LOCATION

# EQUIPMENT TABLES

# EQUI - Equipment Master Data
  - name: "extract-everest-EQUI"
    database: "db-databricks-raw"
    query: >
      SELECT *
      FROM hub_dev.g_external.v_cognite_equi_everest
      WHERE coalesce(
              try_to_timestamp(DATETIMESTAMP, 'yyyy-MM-dd HH:mm:ss.SSSSSS'),
              try_to_timestamp(DATETIMESTAMP, 'yyyy-MM-dd HH:mm:ss')
            ) >= current_timestamp() - INTERVAL 1 YEAR
    destination:
      type: "raw"
      database: "db_databricks_everest_raw"
      table: "tb_EQUI"
    primary-key: "{EQUNR}"

# EQUZ - Equipment Time Segment
  - name: "extract-everest-EQUZ"
    database: "db-databricks-raw"
    query: >
      SELECT CONCAT(EQUNR, '_', DATBI, '_', EQLFN) AS EQUZ_PK, *
      FROM hub_dev.g_external.v_cognite_equz_everest
      WHERE coalesce(
              try_to_timestamp(DATETIMESTAMP, 'yyyy-MM-dd HH:mm:ss.SSSSSS'),
              try_to_timestamp(DATETIMESTAMP, 'yyyy-MM-dd HH:mm:ss')
            ) >= current_timestamp() - INTERVAL 1 YEAR
    destination:
      type: "raw"
      database: "db_databricks_everest_raw"
      table: "tb_EQUZ"
    primary-key: "{EQUZ_PK}"

# EQKT - Equipment Short Texts
  - name: "extract-everest-EQKT"
    database: "db-databricks-raw"
    query: >
      SELECT CONCAT(EQUNR, '_', SPRAS) AS EQKT_PK, *
      FROM hub_dev.g_external.v_cognite_eqkt_everest
      WHERE coalesce(
              try_to_timestamp(DATETIMESTAMP, 'yyyy-MM-dd HH:mm:ss.SSSSSS'),
              try_to_timestamp(DATETIMESTAMP, 'yyyy-MM-dd HH:mm:ss')
            ) >= current_timestamp() - INTERVAL 1 YEAR
    destination:
      type: "raw"
      database: "db_databricks_everest_raw"
      table: "tb_EQKT"
    primary-key: "{EQKT_PK}"

# EQST - Equipment to BOM Link
  - name: "extract-everest-EQST"
    database: "db-databricks-raw"
    query: >
      SELECT CONCAT(EQUNR, '_', WERKS, '_', STLAN, '_', STLNR, '_', STLAL) AS EQST_PK, *
      FROM hub_dev.g_external.v_cognite_eqst_everest
      WHERE coalesce(
              try_to_timestamp(DATETIMESTAMP, 'yyyy-MM-dd HH:mm:ss.SSSSSS'),
              try_to_timestamp(DATETIMESTAMP, 'yyyy-MM-dd HH:mm:ss')
            ) >= current_timestamp() - INTERVAL 1 YEAR
    destination:
      type: "raw"
      database: "db_databricks_everest_raw"
      table: "tb_EQST"
    primary-key: "{EQST_PK}"

# END EQUIPMENT

# MEASURING POINT & COUNTER TABLES

# IMPTT - Measuring Point
  - name: "extract-everest-IMPTT"
    database: "db-databricks-raw"
    query: >
      SELECT *
      FROM hub_dev.g_external.v_cognite_imptt_everest
      WHERE coalesce(
              try_to_timestamp(DATETIMESTAMP, 'yyyy-MM-dd HH:mm:ss.SSSSSS'),
              try_to_timestamp(DATETIMESTAMP, 'yyyy-MM-dd HH:mm:ss')
            ) >= current_timestamp() - INTERVAL 1 YEAR
    destination:
      type: "raw"
      database: "db_databricks_everest_raw"
      table: "tb_IMPTT"
    primary-key: "{POINT}"

# IMRG - Measurement Document
  - name: "extract-everest-IMRG"
    database: "db-databricks-raw"
    query: >
      SELECT *
      FROM hub_dev.g_external.v_cognite_imrg_everest
      WHERE coalesce(
              try_to_timestamp(DATETIMESTAMP, 'yyyy-MM-dd HH:mm:ss.SSSSSS'),
              try_to_timestamp(DATETIMESTAMP, 'yyyy-MM-dd HH:mm:ss')
            ) >= current_timestamp() - INTERVAL 1 YEAR
    destination:
      type: "raw"
      database: "db_databricks_everest_raw"
      table: "tb_IMRG"
    primary-key: "{MDOCM}"

# END MEASURING POINT

# MAINTENANCE PLANNING TABLES

# MPLA - Maintenance Plan
  - name: "extract-everest-MPLA"
    database: "db-databricks-raw"
    query: >
      SELECT *
      FROM hub_dev.g_external.v_cognite_mpla_everest
      WHERE coalesce(
              try_to_timestamp(DATETIMESTAMP, 'yyyy-MM-dd HH:mm:ss.SSSSSS'),
              try_to_timestamp(DATETIMESTAMP, 'yyyy-MM-dd HH:mm:ss')
            ) >= current_timestamp() - INTERVAL 1 YEAR
    destination:
      type: "raw"
      database: "db_databricks_everest_raw"
      table: "tb_MPLA"
    primary-key: "{WARPL}"

# MPOS - Maintenance Item
  - name: "extract-everest-MPOS"
    database: "db-databricks-raw"
    query: >
      SELECT CONCAT(WARPL, '_', WAPOS) AS MPOS_PK, *
      FROM hub_dev.g_external.v_cognite_mpos_everest
      WHERE coalesce(
              try_to_timestamp(DATETIMESTAMP, 'yyyy-MM-dd HH:mm:ss.SSSSSS'),
              try_to_timestamp(DATETIMESTAMP, 'yyyy-MM-dd HH:mm:ss')
            ) >= current_timestamp() - INTERVAL 1 YEAR
    destination:
      type: "raw"
      database: "db_databricks_everest_raw"
      table: "tb_MPOS"
    primary-key: "{MPOS_PK}"

# MHIO - Maintenance Order Call Object
  - name: "extract-everest-MHIO"
    database: "db-databricks-raw"
    query: >
      SELECT CONCAT(WARPL, '_', WAPOS, '_', ABNUM, '_', LFDAT) AS MHIO_PK, *
      FROM hub_dev.g_external.v_cognite_mhio_everest
      WHERE coalesce(
              try_to_timestamp(DATETIMESTAMP, 'yyyy-MM-dd HH:mm:ss.SSSSSS'),
              try_to_timestamp(DATETIMESTAMP, 'yyyy-MM-dd HH:mm:ss')
            ) >= current_timestamp() - INTERVAL 1 YEAR
    destination:
      type: "raw"
      database: "db_databricks_everest_raw"
      table: "tb_MHIO"
    primary-key: "{MHIO_PK}"

# MHIS - Maintenance Plan History
  - name: "extract-everest-MHIS"
    database: "db-databricks-raw"
    query: >
      SELECT CONCAT(WARPL, '_', ABNUM, '_', ZAEHL) AS MHIS_PK, *
      FROM hub_dev.g_external.v_cognite_mhis_everest
      WHERE coalesce(
              try_to_timestamp(DATETIMESTAMP, 'yyyy-MM-dd HH:mm:ss.SSSSSS'),
              try_to_timestamp(DATETIMESTAMP, 'yyyy-MM-dd HH:mm:ss')
            ) >= current_timestamp() - INTERVAL 1 YEAR
    destination:
      type: "raw"
      database: "db_databricks_everest_raw"
      table: "tb_MHIS"
    primary-key: "{MHIS_PK}"

# END MAINTENANCE PLANNING

# ORDER TABLES (PM/PP)

# AUFK - Order Master Data
  - name: "extract-everest-AUFK"
    database: "db-databricks-raw"
    query: >
      SELECT *
      FROM hub_dev.g_external.v_cognite_aufk_everest
      WHERE coalesce(
              try_to_timestamp(DATETIMESTAMP, 'yyyy-MM-dd HH:mm:ss.SSSSSS'),
              try_to_timestamp(DATETIMESTAMP, 'yyyy-MM-dd HH:mm:ss')
            ) >= current_timestamp() - INTERVAL 1 YEAR
    destination:
      type: "raw"
      database: "db_databricks_everest_raw"
      table: "tb_AUFK"
    primary-key: "{AUFNR}"

# AFIH - Maintenance Order Header
  - name: "extract-everest-AFIH"
    database: "db-databricks-raw"
    query: >
      SELECT *
      FROM hub_dev.g_external.v_cognite_afih_everest
      WHERE coalesce(
              try_to_timestamp(DATETIMESTAMP, 'yyyy-MM-dd HH:mm:ss.SSSSSS'),
              try_to_timestamp(DATETIMESTAMP, 'yyyy-MM-dd HH:mm:ss')
            ) >= current_timestamp() - INTERVAL 1 YEAR
    destination:
      type: "raw"
      database: "db_databricks_everest_raw"
      table: "tb_AFIH"
    primary-key: "{AUFNR}"

# AFKO - Order Header Data PP Orders
  - name: "extract-everest-AFKO"
    database: "db-databricks-raw"
    query: >
      SELECT *
      FROM hub_dev.g_external.v_cognite_afko_everest
      WHERE coalesce(
              try_to_timestamp(DATETIMESTAMP, 'yyyy-MM-dd HH:mm:ss.SSSSSS'),
              try_to_timestamp(DATETIMESTAMP, 'yyyy-MM-dd HH:mm:ss')
            ) >= current_timestamp() - INTERVAL 1 YEAR
    destination:
      type: "raw"
      database: "db_databricks_everest_raw"
      table: "tb_AFKO"
    primary-key: "{AUFNR}"

# AFPO - Order Item
  - name: "extract-everest-AFPO"
    database: "db-databricks-raw"
    query: >
      SELECT CONCAT(AUFNR, '_', POSNR) AS AFPO_PK, *
      FROM hub_dev.g_external.v_cognite_afpo_everest
      WHERE coalesce(
              try_to_timestamp(DATETIMESTAMP, 'yyyy-MM-dd HH:mm:ss.SSSSSS'),
              try_to_timestamp(DATETIMESTAMP, 'yyyy-MM-dd HH:mm:ss')
            ) >= current_timestamp() - INTERVAL 1 YEAR
    destination:
      type: "raw"
      database: "db_databricks_everest_raw"
      table: "tb_AFPO"
    primary-key: "{AFPO_PK}"

# AFVC - Order Operations
  - name: "extract-everest-AFVC"
    database: "db-databricks-raw"
    query: >
      SELECT CONCAT(AUFPL, '_', APLZL) AS AFVC_PK, *
      FROM hub_dev.g_external.v_cognite_afvc_everest
      WHERE coalesce(
              try_to_timestamp(DATETIMESTAMP, 'yyyy-MM-dd HH:mm:ss.SSSSSS'),
              try_to_timestamp(DATETIMESTAMP, 'yyyy-MM-dd HH:mm:ss')
            ) >= current_timestamp() - INTERVAL 1 YEAR
    destination:
      type: "raw"
      database: "db_databricks_everest_raw"
      table: "tb_AFVC"
    primary-key: "{AFVC_PK}"

# AFVV - Order Operation Quantities, Dates and Values
  - name: "extract-everest-AFVV"
    database: "db-databricks-raw"
    query: >
      SELECT CONCAT(AUFPL, '_', APLZL) AS AFVV_PK, *
      FROM hub_dev.g_external.v_cognite_afvv_everest
      WHERE coalesce(
              try_to_timestamp(DATETIMESTAMP, 'yyyy-MM-dd HH:mm:ss.SSSSSS'),
              try_to_timestamp(DATETIMESTAMP, 'yyyy-MM-dd HH:mm:ss')
            ) >= current_timestamp() - INTERVAL 1 YEAR
    destination:
      type: "raw"
      database: "db_databricks_everest_raw"
      table: "tb_AFVV"
    primary-key: "{AFVV_PK}"

# AFRU - Order Confirmations
  - name: "extract-everest-AFRU"
    database: "db-databricks-raw"
    query: >
      SELECT CONCAT(RUECK, '_', RMZHL) AS AFRU_PK, *
      FROM hub_dev.g_external.v_cognite_afru_everest
      WHERE coalesce(
              try_to_timestamp(DATETIMESTAMP, 'yyyy-MM-dd HH:mm:ss.SSSSSS'),
              try_to_timestamp(DATETIMESTAMP, 'yyyy-MM-dd HH:mm:ss')
            ) >= current_timestamp() - INTERVAL 1 YEAR
    destination:
      type: "raw"
      database: "db_databricks_everest_raw"
      table: "tb_AFRU"
    primary-key: "{AFRU_PK}"

# AFRC - Incorrect Confirmations
  - name: "extract-everest-AFRC"
    database: "db-databricks-raw"
    query: >
      SELECT CONCAT(AUFNR, '_', RUECK, '_', RMZHL) AS AFRC_PK, *
      FROM hub_dev.g_external.v_cognite_afrc_everest
      WHERE coalesce(
              try_to_timestamp(DATETIMESTAMP, 'yyyy-MM-dd HH:mm:ss.SSSSSS'),
              try_to_timestamp(DATETIMESTAMP, 'yyyy-MM-dd HH:mm:ss')
            ) >= current_timestamp() - INTERVAL 1 YEAR
    destination:
      type: "raw"
      database: "db_databricks_everest_raw"
      table: "tb_AFRC"
    primary-key: "{AFRC_PK}"

# AFFW - Goods Movements with Errors from Confirmations
  - name: "extract-everest-AFFW"
    database: "db-databricks-raw"
    query: >          
      SELECT CONCAT(WEBLNR, '_', WEBLPOS) AS AFFW_PK, *
      FROM hub_dev.g_external.v_cognite_affw_everest
      WHERE coalesce(
              try_to_timestamp(DATETIMESTAMP, 'yyyy-MM-dd HH:mm:ss.SSSSSS'),
              try_to_timestamp(DATETIMESTAMP, 'yyyy-MM-dd HH:mm:ss')
            ) >= current_timestamp() - INTERVAL 1 YEAR
    destination:
      type: "raw"
      database: "db_databricks_everest_raw"
      table: "tb_AFFW"
    primary-key: "{AFFW_PK}"

# AFWI - Subsequently Posted Goods Movements for Confirmations
  - name: "extract-everest-AFWI"
    database: "db-databricks-raw"
    query: >
      SELECT CONCAT(RUECK, '_', RMZHL, '_', MBLNR, '_', MJAHR, '_', MBLPO) AS AFWI_PK, *
      FROM hub_dev.g_external.v_cognite_afwi_everest
      WHERE coalesce(
              try_to_timestamp(DATETIMESTAMP, 'yyyy-MM-dd HH:mm:ss.SSSSSS'),
              try_to_timestamp(DATETIMESTAMP, 'yyyy-MM-dd HH:mm:ss')
            ) >= current_timestamp() - INTERVAL 1 YEAR
    destination:
      type: "raw"
      database: "db_databricks_everest_raw"
      table: "tb_AFWI"
    primary-key: "{AFWI_PK}"

# AUFM - Goods Movements for Order
  - name: "extract-everest-AUFM"
    database: "db-databricks-raw"
    query: >
      SELECT CONCAT(MBLNR, '_', MJAHR, '_', ZEILE) AS AUFM_PK, *
      FROM hub_dev.g_external.v_cognite_aufm_everest
      WHERE coalesce(
              try_to_timestamp(DATETIMESTAMP, 'yyyy-MM-dd HH:mm:ss.SSSSSS'),
              try_to_timestamp(DATETIMESTAMP, 'yyyy-MM-dd HH:mm:ss')
            ) >= current_timestamp() - INTERVAL 1 YEAR
    destination:
      type: "raw"
      database: "db_databricks_everest_raw"
      table: "tb_AUFM"
    primary-key: "{AUFM_PK}"

# END ORDER

# CLASSIFICATION & STATUS TABLES

# AUSP - Characteristic Values
  - name: "extract-everest-AUSP"
    database: "db-databricks-raw"
    query: >
      SELECT CONCAT(OBJEK, '_', ATINN, '_', ATZHL, '_', MAFID, '_', KLART, '_', ADZHL) AS AUSP_PK, *
      FROM hub_dev.g_external.v_cognite_ausp_everest
      WHERE coalesce(
              try_to_timestamp(DATETIMESTAMP, 'yyyy-MM-dd HH:mm:ss.SSSSSS'),
              try_to_timestamp(DATETIMESTAMP, 'yyyy-MM-dd HH:mm:ss')
            ) >= current_timestamp() - INTERVAL 1 YEAR
    destination:
      type: "raw"
      database: "db_databricks_everest_raw"
      table: "tb_AUSP"
    primary-key: "{AUSP_PK}"

# KSSK - Allocation Table: Object to Class
  - name: "extract-everest-KSSK"
    database: "db-databricks-raw"
    query: >
      SELECT CONCAT(OBJEK, '_', MAFID, '_', KLART, '_', ADZHL) AS KSSK_PK, *
      FROM hub_dev.g_external.v_cognite_kssk_everest
      WHERE coalesce(
              try_to_timestamp(DATETIMESTAMP, 'yyyy-MM-dd HH:mm:ss.SSSSSS'),
              try_to_timestamp(DATETIMESTAMP, 'yyyy-MM-dd HH:mm:ss')
            ) >= current_timestamp() - INTERVAL 1 YEAR
    destination:
      type: "raw"
      database: "db_databricks_everest_raw"
      table: "tb_KSSK"
    primary-key: "{KSSK_PK}"

# JEST - Individual Object Status
  - name: "extract-everest-JEST"
    database: "db-databricks-raw"
    query: >
      SELECT CONCAT(OBJNR, '_', STAT) AS JEST_PK, *
      FROM hub_dev.g_external.v_cognite_jest_everest
      WHERE coalesce(
              try_to_timestamp(DATETIMESTAMP, 'yyyy-MM-dd HH:mm:ss.SSSSSS'),
              try_to_timestamp(DATETIMESTAMP, 'yyyy-MM-dd HH:mm:ss')
            ) >= current_timestamp() - INTERVAL 1 YEAR
    destination:
      type: "raw"
      database: "db_databricks_everest_raw"
      table: "tb_JEST"
    primary-key: "{JEST_PK}"

# END CLASSIFICATION & STATUS

# COSTING & CONTROLLING TABLES

# CKIS - Items Unit Costing and Itemization
  - name: "extract-everest-CKIS"
    database: "db-databricks-raw"
    query: >
      SELECT CONCAT(LEDNR, '_', BZOBJ, '_', KALNR, '_', KALKA, '_', KADKY, '_', TVERS, '_', BWVAR, '_', KKZMA, '_', POSNR) AS CKIS_PK, *
      FROM hub_dev.g_external.v_cognite_ckis_everest
      WHERE coalesce(
              try_to_timestamp(DATETIMESTAMP, 'yyyy-MM-dd HH:mm:ss.SSSSSS'),
              try_to_timestamp(DATETIMESTAMP, 'yyyy-MM-dd HH:mm:ss')
            ) >= current_timestamp() - INTERVAL 1 YEAR
    destination:
      type: "raw"
      database: "db_databricks_everest_raw"
      table: "tb_CKIS"
    primary-key: "{CKIS_PK}"

# COEP - CO Object: Line Items by Period
  - name: "extract-everest-COEP"
    database: "db-databricks-raw"
    query: >
      SELECT CONCAT(KOKRS, '_', BELNR, '_', BUZEI) AS COEP_PK, *
      FROM hub_dev.g_external.v_cognite_coep_everest
      WHERE coalesce(
              try_to_timestamp(DATETIMESTAMP, 'yyyy-MM-dd HH:mm:ss.SSSSSS'),
              try_to_timestamp(DATETIMESTAMP, 'yyyy-MM-dd HH:mm:ss')
            ) >= current_timestamp() - INTERVAL 1 YEAR
    destination:
      type: "raw"
      database: "db_databricks_everest_raw"
      table: "tb_COEP"
    primary-key: "{COEP_PK}"

# COSP - CO Object: Cost Totals for External Postings
  - name: "extract-everest-COSP"
    database: "db-databricks-raw"
    query: >
      SELECT CONCAT(LEDNR, '_', OBJNR, '_', GJAHR, '_', WRTTP, '_', VERSN, '_', KSTAR, '_', HRKFT, '_', VRGNG, '_', VBUND, '_', PARGB, '_', BEKNZ, '_', TWAER, '_', PERBL) AS COSP_PK, *
      FROM hub_dev.g_external.v_cognite_cosp_everest
      WHERE coalesce(
              try_to_timestamp(DATETIMESTAMP, 'yyyy-MM-dd HH:mm:ss.SSSSSS'),
              try_to_timestamp(DATETIMESTAMP, 'yyyy-MM-dd HH:mm:ss')
            ) >= current_timestamp() - INTERVAL 1 YEAR
    destination:
      type: "raw"
      database: "db_databricks_everest_raw"
      table: "tb_COSP"
    primary-key: "{COSP_PK}"

# KEKO - Product Costing Header Data
  - name: "extract-everest-KEKO"
    database: "db-databricks-raw"
    query: >
      SELECT CONCAT(BZOBJ, '_', KALNR, '_', KALKA, '_', KADKY, '_', TVERS, '_', BWVAR, '_', KKZMA) AS KEKO_PK, *
      FROM hub_dev.g_external.v_cognite_keko_everest
      WHERE coalesce(
              try_to_timestamp(DATETIMESTAMP, 'yyyy-MM-dd HH:mm:ss.SSSSSS'),
              try_to_timestamp(DATETIMESTAMP, 'yyyy-MM-dd HH:mm:ss')
            ) >= current_timestamp() - INTERVAL 1 YEAR
    destination:
      type: "raw"
      database: "db_databricks_everest_raw"
      table: "tb_KEKO"
    primary-key: "{KEKO_PK}"

# PMCO - Cost Structure of Maintenance Order
  - name: "extract-everest-PMCO"
    database: "db-databricks-raw"
    query: >
      SELECT CONCAT(OBJNR, '_', BELTP, '_', WRTTP, '_', GJAHR, '_', PERIO, '_', F_OBJNR) AS PMCO_PK, *
      FROM hub_dev.g_external.v_cognite_pmco_everest
      WHERE coalesce(
              try_to_timestamp(DATETIMESTAMP, 'yyyy-MM-dd HH:mm:ss.SSSSSS'),
              try_to_timestamp(DATETIMESTAMP, 'yyyy-MM-dd HH:mm:ss')
            ) >= current_timestamp() - INTERVAL 1 YEAR
    destination:
      type: "raw"
      database: "db_databricks_everest_raw"
      table: "tb_PMCO"
    primary-key: "{PMCO_PK}"

# END COSTING & CONTROLLING

# TASK LIST & ROUTING TABLES

# PLKO - Task List Header
  - name: "extract-everest-PLKO"
    database: "db-databricks-raw"
    query: >
      SELECT CONCAT(PLNTY, '_', PLNNR, '_', PLNAL) AS PLKO_PK, *
      FROM hub_dev.g_external.v_cognite_plko_everest
      WHERE coalesce(
              try_to_timestamp(DATETIMESTAMP, 'yyyy-MM-dd HH:mm:ss.SSSSSS'),
              try_to_timestamp(DATETIMESTAMP, 'yyyy-MM-dd HH:mm:ss')
            ) >= current_timestamp() - INTERVAL 1 YEAR
    destination:
      type: "raw"
      database: "db_databricks_everest_raw"
      table: "tb_PLKO"
    primary-key: "{PLKO_PK}"

# PLAS - Task List Selection of Operations
  - name: "extract-everest-PLAS"
    database: "db-databricks-raw"
    query: >
      SELECT CONCAT(PLNTY, '_', PLNNR, '_', PLNKN, '_', ZAEHL) AS PLAS_PK, *
      FROM hub_dev.g_external.v_cognite_plas_everest
      WHERE coalesce(
              try_to_timestamp(DATETIMESTAMP, 'yyyy-MM-dd HH:mm:ss.SSSSSS'),
              try_to_timestamp(DATETIMESTAMP, 'yyyy-MM-dd HH:mm:ss')
            ) >= current_timestamp() - INTERVAL 1 YEAR
    destination:
      type: "raw"
      database: "db_databricks_everest_raw"
      table: "tb_PLAS"
    primary-key: "{PLAS_PK}"

# PLPO - Task List Operation or Activity
  - name: "extract-everest-PLPO"
    database: "db-databricks-raw"
    query: >
      SELECT CONCAT(PLNTY, '_', PLNNR, '_', PLNKN) AS PLPO_PK, *
      FROM hub_dev.g_external.v_cognite_plpo_everest
      WHERE coalesce(
              try_to_timestamp(DATETIMESTAMP, 'yyyy-MM-dd HH:mm:ss.SSSSSS'),
              try_to_timestamp(DATETIMESTAMP, 'yyyy-MM-dd HH:mm:ss')
            ) >= current_timestamp() - INTERVAL 1 YEAR
    destination:
      type: "raw"
      database: "db_databricks_everest_raw"
      table: "tb_PLPO"
    primary-key: "{PLPO_PK}"

# PLFL - Task List Sequences
  - name: "extract-everest-PLFL"
    database: "db-databricks-raw"
    query: >
      SELECT CONCAT(PLNTY, '_', PLNNR, '_', PLNAL, '_', PLNFL, '_', ZAEHL) AS PLFL_PK, *
      FROM hub_dev.g_external.v_cognite_plfl_everest
      WHERE coalesce(
              try_to_timestamp(DATETIMESTAMP, 'yyyy-MM-dd HH:mm:ss.SSSSSS'),
              try_to_timestamp(DATETIMESTAMP, 'yyyy-MM-dd HH:mm:ss')
            ) >= current_timestamp() - INTERVAL 1 YEAR
    destination:
      type: "raw"
      database: "db_databricks_everest_raw"
      table: "tb_PLFL"
    primary-key: "{PLFL_PK}"

# PLMZ - Allocation of BOM Items to Operations
  - name: "extract-everest-PLMZ"
    database: "db-databricks-raw"
    query: >
      SELECT CONCAT(PLNTY, '_', PLNNR, '_', ZUONR, '_', ZAEHL) AS PLMZ_PK, *
      FROM hub_dev.g_external.v_cognite_plmz_everest
      WHERE coalesce(
              try_to_timestamp(DATETIMESTAMP, 'yyyy-MM-dd HH:mm:ss.SSSSSS'),
              try_to_timestamp(DATETIMESTAMP, 'yyyy-MM-dd HH:mm:ss')
            ) >= current_timestamp() - INTERVAL 1 YEAR
    destination:
      type: "raw"
      database: "db_databricks_everest_raw"
      table: "tb_PLMZ"
    primary-key: "{PLMZ_PK}"

# PLWP - Allocation of Maintenance Packages to Task List Operations
  - name: "extract-everest-PLWP"
    database: "db-databricks-raw"
    query: >
      SELECT CONCAT(PLNTY, '_', PLNNR, '_', PLNAL, '_', PLNFL, '_', PLNKN, '_', PAKET, '_', ZAEHL) AS PLWP_PK, *
      FROM hub_dev.g_external.v_cognite_plwp_everest
      WHERE coalesce(
              try_to_timestamp(DATETIMESTAMP, 'yyyy-MM-dd HH:mm:ss.SSSSSS'),
              try_to_timestamp(DATETIMESTAMP, 'yyyy-MM-dd HH:mm:ss')
            ) >= current_timestamp() - INTERVAL 1 YEAR
    destination:
      type: "raw"
      database: "db_databricks_everest_raw"
      table: "tb_PLWP"
    primary-key: "{PLWP_PK}"

# MAPL - Assignment of Task Lists to Materials
  - name: "extract-everest-MAPL"
    database: "db-databricks-raw"
    query: >
      SELECT CONCAT(MATNR, '_', WERKS, '_', PLNTY, '_', PLNNR, '_', PLNAL, '_', ZAEHL) AS MAPL_PK, *
      FROM hub_dev.g_external.v_cognite_mapl_everest
      WHERE coalesce(
              try_to_timestamp(DATETIMESTAMP, 'yyyy-MM-dd HH:mm:ss.SSSSSS'),
              try_to_timestamp(DATETIMESTAMP, 'yyyy-MM-dd HH:mm:ss')
            ) >= current_timestamp() - INTERVAL 1 YEAR
    destination:
      type: "raw"
      database: "db_databricks_everest_raw"
      table: "tb_MAPL"
    primary-key: "{MAPL_PK}"

# EAPL - Allocation of Task Lists to Equipment
  - name: "extract-everest-EAPL"
    database: "db-databricks-raw"
    query: >
      SELECT CONCAT(EQUNR, '_', PLNTY, '_', PLNNR, '_', PLNAL, '_', ZKRIZ, '_', ZAEHL) AS EAPL_PK, *
      FROM hub_dev.g_external.v_cognite_eapl_everest
      WHERE coalesce(
              try_to_timestamp(DATETIMESTAMP, 'yyyy-MM-dd HH:mm:ss.SSSSSS'),
              try_to_timestamp(DATETIMESTAMP, 'yyyy-MM-dd HH:mm:ss')
            ) >= current_timestamp() - INTERVAL 1 YEAR
    destination:
      type: "raw"
      database: "db_databricks_everest_raw"
      table: "tb_EAPL"
    primary-key: "{EAPL_PK}"

# END TASK LIST & ROUTING

# BILL OF MATERIAL TABLES

# MAST - Material to BOM Link
  - name: "extract-everest-MAST"
    database: "db-databricks-raw"
    query: >
      SELECT CONCAT(MATNR, '_', WERKS, '_', STLAN, '_', STLNR, '_', STLAL) AS MAST_PK, *
      FROM hub_dev.g_external.v_cognite_mast_everest
      WHERE coalesce(
              try_to_timestamp(DATETIMESTAMP, 'yyyy-MM-dd HH:mm:ss.SSSSSS'),
              try_to_timestamp(DATETIMESTAMP, 'yyyy-MM-dd HH:mm:ss')
            ) >= current_timestamp() - INTERVAL 1 YEAR
    destination:
      type: "raw"
      database: "db_databricks_everest_raw"
      table: "tb_MAST"
    primary-key: "{MAST_PK}"

# STKO - BOM Header
  - name: "extract-everest-STKO"
    database: "db-databricks-raw"
    query: >
      SELECT CONCAT(STLTY, '_', STLNR, '_', STLAL, '_', STKOZ) AS STKO_PK, *
      FROM hub_dev.g_external.v_cognite_stko_everest
      WHERE coalesce(
              try_to_timestamp(DATETIMESTAMP, 'yyyy-MM-dd HH:mm:ss.SSSSSS'),
              try_to_timestamp(DATETIMESTAMP, 'yyyy-MM-dd HH:mm:ss')
            ) >= current_timestamp() - INTERVAL 1 YEAR
    destination:
      type: "raw"
      database: "db_databricks_everest_raw"
      table: "tb_STKO"
    primary-key: "{STKO_PK}"

# STPO - BOM Item
  - name: "extract-everest-STPO"
    database: "db-databricks-raw"
    query: >
      SELECT CONCAT(STLTY, '_', STLNR, '_', STLKN, '_', STPOZ) AS STPO_PK, *
      FROM hub_dev.g_external.v_cognite_stpo_everest
      WHERE coalesce(
              try_to_timestamp(DATETIMESTAMP, 'yyyy-MM-dd HH:mm:ss.SSSSSS'),
              try_to_timestamp(DATETIMESTAMP, 'yyyy-MM-dd HH:mm:ss')
            ) >= current_timestamp() - INTERVAL 1 YEAR
    destination:
      type: "raw"
      database: "db_databricks_everest_raw"
      table: "tb_STPO"
    primary-key: "{STPO_PK}"

# STAS - BOMs Item Selection
  - name: "extract-everest-STAS"
    database: "db-databricks-raw"
    query: >
      SELECT CONCAT(STLTY, '_', STLNR, '_', STLAL, '_', STLKN, '_', STASZ) AS STAS_PK, *
      FROM hub_dev.g_external.v_cognite_stas_everest
      WHERE coalesce(
              try_to_timestamp(DATETIMESTAMP, 'yyyy-MM-dd HH:mm:ss.SSSSSS'),
              try_to_timestamp(DATETIMESTAMP, 'yyyy-MM-dd HH:mm:ss')
            ) >= current_timestamp() - INTERVAL 1 YEAR
    destination:
      type: "raw"
      database: "db_databricks_everest_raw"
      table: "tb_STAS"
    primary-key: "{STAS_PK}"

# END BILL OF MATERIAL

# MATERIAL MASTER TABLES

# MARA - General Material Data
  - name: "extract-everest-MARA"
    database: "db-databricks-raw"
    query: >
      SELECT *
      FROM hub_dev.g_external.v_cognite_mara_everest
      WHERE coalesce(
              try_to_timestamp(DATETIMESTAMP, 'yyyy-MM-dd HH:mm:ss.SSSSSS'),
              try_to_timestamp(DATETIMESTAMP, 'yyyy-MM-dd HH:mm:ss')
            ) >= current_timestamp() - INTERVAL 1 YEAR
    destination:
      type: "raw"
      database: "db_databricks_everest_raw"
      table: "tb_MARA"
    primary-key: "{MATNR}"

# MAKT - Material Descriptions
  - name: "extract-everest-MAKT"
    database: "db-databricks-raw"
    query: >
      SELECT CONCAT(MATNR, '_', SPRAS) AS MAKT_PK, *
      FROM hub_dev.g_external.v_cognite_makt_everest
      WHERE coalesce(
              try_to_timestamp(DATETIMESTAMP, 'yyyy-MM-dd HH:mm:ss.SSSSSS'),
              try_to_timestamp(DATETIMESTAMP, 'yyyy-MM-dd HH:mm:ss')
            ) >= current_timestamp() - INTERVAL 1 YEAR
    destination:
      type: "raw"
      database: "db_databricks_everest_raw"
      table: "tb_MAKT"
    primary-key: "{MAKT_PK}"

# MARC - Plant Data for Material
  - name: "extract-everest-MARC"
    database: "db-databricks-raw"
    query: >
      SELECT CONCAT(MATNR, '_', WERKS) AS MARC_PK, *
      FROM hub_dev.g_external.v_cognite_marc_everest
      WHERE coalesce(
              try_to_timestamp(DATETIMESTAMP, 'yyyy-MM-dd HH:mm:ss.SSSSSS'),
              try_to_timestamp(DATETIMESTAMP, 'yyyy-MM-dd HH:mm:ss')
            ) >= current_timestamp() - INTERVAL 1 YEAR
    destination:
      type: "raw"
      database: "db_databricks_everest_raw"
      table: "tb_MARC"
    primary-key: "{MARC_PK}"

# MARD - Storage Location Data for Material
  - name: "extract-everest-MARD"
    database: "db-databricks-raw"
    query: >
      SELECT CONCAT(MATNR, '_', WERKS, '_', LGORT) AS MARD_PK, *
      FROM hub_dev.g_external.v_cognite_mard_everest
      WHERE coalesce(
              try_to_timestamp(DATETIMESTAMP, 'yyyy-MM-dd HH:mm:ss.SSSSSS'),
              try_to_timestamp(DATETIMESTAMP, 'yyyy-MM-dd HH:mm:ss')
            ) >= current_timestamp() - INTERVAL 1 YEAR
    destination:
      type: "raw"
      database: "db_databricks_everest_raw"
      table: "tb_MARD"
    primary-key: "{MARD_PK}"

# MARM - Units of Measure for Material
  - name: "extract-everest-MARM"
    database: "db-databricks-raw"
    query: >
      SELECT CONCAT(MATNR, '_', MEINH) AS MARM_PK, *
      FROM hub_dev.g_external.v_cognite_marm_everest
      WHERE coalesce(
              try_to_timestamp(DATETIMESTAMP, 'yyyy-MM-dd HH:mm:ss.SSSSSS'),
              try_to_timestamp(DATETIMESTAMP, 'yyyy-MM-dd HH:mm:ss')
            ) >= current_timestamp() - INTERVAL 1 YEAR
    destination:
      type: "raw"
      database: "db_databricks_everest_raw"
      table: "tb_MARM"
    primary-key: "{MARM_PK}"

# MBEW - Material Valuation
  - name: "extract-everest-MBEW"
    database: "db-databricks-raw"
    query: >
      SELECT CONCAT(MATNR, '_', BWKEY, '_', BWTAR) AS MBEW_PK, *
      FROM hub_dev.g_external.v_cognite_mbew_everest
      WHERE coalesce(
              try_to_timestamp(DATETIMESTAMP, 'yyyy-MM-dd HH:mm:ss.SSSSSS'),
              try_to_timestamp(DATETIMESTAMP, 'yyyy-MM-dd HH:mm:ss')
            ) >= current_timestamp() - INTERVAL 1 YEAR
    destination:
      type: "raw"
      database: "db_databricks_everest_raw"
      table: "tb_MBEW"
    primary-key: "{MBEW_PK}"

# MKAL - Production Versions of Material
  - name: "extract-everest-MKAL"
    database: "db-databricks-raw"
    query: >
      SELECT CONCAT(MATNR, '_', WERKS, '_', VERID) AS MKAL_PK, *
      FROM hub_dev.g_external.v_cognite_mkal_everest
      WHERE coalesce(
              try_to_timestamp(DATETIMESTAMP, 'yyyy-MM-dd HH:mm:ss.SSSSSS'),
              try_to_timestamp(DATETIMESTAMP, 'yyyy-MM-dd HH:mm:ss')
            ) >= current_timestamp() - INTERVAL 1 YEAR
    destination:
      type: "raw"
      database: "db_databricks_everest_raw"
      table: "tb_MKAL"
    primary-key: "{MKAL_PK}"

# END MATERIAL MASTER

# BATCH MANAGEMENT TABLES

# MCH1 - Batches Cross-Plant
  - name: "extract-everest-MCH1"
    database: "db-databricks-raw"
    query: >
      SELECT CONCAT(MATNR, '_', CHARG) AS MCH1_PK, *
      FROM hub_dev.g_external.v_cognite_mch1_everest
      WHERE coalesce(
              try_to_timestamp(DATETIMESTAMP, 'yyyy-MM-dd HH:mm:ss.SSSSSS'),
              try_to_timestamp(DATETIMESTAMP, 'yyyy-MM-dd HH:mm:ss')
            ) >= current_timestamp() - INTERVAL 1 YEAR
    destination:
      type: "raw"
      database: "db_databricks_everest_raw"
      table: "tb_MCH1"
    primary-key: "{MCH1_PK}"

# MCHA - Batches
  - name: "extract-everest-MCHA"
    database: "db-databricks-raw"
    query: >
      SELECT CONCAT(MATNR, '_', WERKS, '_', CHARG) AS MCHA_PK, *
      FROM hub_dev.g_external.v_cognite_mcha_everest
      WHERE coalesce(
              try_to_timestamp(DATETIMESTAMP, 'yyyy-MM-dd HH:mm:ss.SSSSSS'),
              try_to_timestamp(DATETIMESTAMP, 'yyyy-MM-dd HH:mm:ss')
            ) >= current_timestamp() - INTERVAL 1 YEAR
    destination:
      type: "raw"
      database: "db_databricks_everest_raw"
      table: "tb_MCHA"
    primary-key: "{MCHA_PK}"

# MCHB - Batch Stocks
  - name: "extract-everest-MCHB"
    database: "db-databricks-raw"
    query: >
      SELECT CONCAT(MATNR, '_', WERKS, '_', LGORT, '_', CHARG) AS MCHB_PK, *
      FROM hub_dev.g_external.v_cognite_mchb_everest
      WHERE coalesce(
              try_to_timestamp(DATETIMESTAMP, 'yyyy-MM-dd HH:mm:ss.SSSSSS'),
              try_to_timestamp(DATETIMESTAMP, 'yyyy-MM-dd HH:mm:ss')
            ) >= current_timestamp() - INTERVAL 1 YEAR
    destination:
      type: "raw"
      database: "db_databricks_everest_raw"
      table: "tb_MCHB"
    primary-key: "{MCHB_PK}"

# END BATCH MANAGEMENT

# INVENTORY & WAREHOUSE TABLES

# LAGP - Storage Bins
  - name: "extract-everest-LAGP"
    database: "db-databricks-raw"
    query: >
      SELECT CONCAT(LGNUM, '_', LGTYP, '_', LGPLA) AS LAGP_PK, *
      FROM hub_dev.g_external.v_cognite_lagp_everest
      WHERE coalesce(
              try_to_timestamp(DATETIMESTAMP, 'yyyy-MM-dd HH:mm:ss.SSSSSS'),
              try_to_timestamp(DATETIMESTAMP, 'yyyy-MM-dd HH:mm:ss')
            ) >= current_timestamp() - INTERVAL 1 YEAR
    destination:
      type: "raw"
      database: "db_databricks_everest_raw"
      table: "tb_LAGP"
    primary-key: "{LAGP_PK}"

# LQUA - Quants
  - name: "extract-everest-LQUA"
    database: "db-databricks-raw"
    query: >
      SELECT *
      FROM hub_dev.g_external.v_cognite_lqua_everest
      WHERE coalesce(
              try_to_timestamp(DATETIMESTAMP, 'yyyy-MM-dd HH:mm:ss.SSSSSS'),
              try_to_timestamp(DATETIMESTAMP, 'yyyy-MM-dd HH:mm:ss')
            ) >= current_timestamp() - INTERVAL 1 YEAR
    destination:
      type: "raw"
      database: "db_databricks_everest_raw"
      table: "tb_LQUA"
    primary-key: "{LQNUM}"

# MSKU - Special Stocks with Customer
  - name: "extract-everest-MSKU"
    database: "db-databricks-raw"
    query: >
      SELECT CONCAT(MATNR, '_', WERKS, '_', CHARG, '_', SOBKZ, '_', KUNNR) AS MSKU_PK, *
      FROM hub_dev.g_external.v_cognite_msku_everest
      WHERE coalesce(
              try_to_timestamp(DATETIMESTAMP, 'yyyy-MM-dd HH:mm:ss.SSSSSS'),
              try_to_timestamp(DATETIMESTAMP, 'yyyy-MM-dd HH:mm:ss')
            ) >= current_timestamp() - INTERVAL 1 YEAR
    destination:
      type: "raw"
      database: "db_databricks_everest_raw"
      table: "tb_MSKU"
    primary-key: "{MSKU_PK}"

# END INVENTORY & WAREHOUSE

# MATERIAL DOCUMENT TABLES

# MKPF - Material Document Header
  - name: "extract-everest-MKPF"
    database: "db-databricks-raw"
    query: >
      SELECT CONCAT(MBLNR, '_', MJAHR) AS MKPF_PK, *
      FROM hub_dev.g_external.v_cognite_mkpf_everest
      WHERE coalesce(
              try_to_timestamp(DATETIMESTAMP, 'yyyy-MM-dd HH:mm:ss.SSSSSS'),
              try_to_timestamp(DATETIMESTAMP, 'yyyy-MM-dd HH:mm:ss')
            ) >= current_timestamp() - INTERVAL 1 YEAR
    destination:
      type: "raw"
      database: "db_databricks_everest_raw"
      table: "tb_MKPF"
    primary-key: "{MKPF_PK}"

# MSEG - Material Document Segment
  - name: "extract-everest-MSEG"
    database: "db-databricks-raw"
    query: >
      SELECT CONCAT(MBLNR, '_', MJAHR, '_', ZEILE) AS MSEG_PK, *
      FROM hub_dev.g_external.v_cognite_mseg_everest
      WHERE coalesce(
              try_to_timestamp(DATETIMESTAMP, 'yyyy-MM-dd HH:mm:ss.SSSSSS'),
              try_to_timestamp(DATETIMESTAMP, 'yyyy-MM-dd HH:mm:ss')
            ) >= current_timestamp() - INTERVAL 1 YEAR
    destination:
      type: "raw"
      database: "db_databricks_everest_raw"
      table: "tb_MSEG"
    primary-key: "{MSEG_PK}"

# END MATERIAL DOCUMENT

# MRP TABLES

# MDKP - Header Data for MRP Document
  - name: "extract-everest-MDKP"
    database: "db-databricks-raw"
    query: >
      SELECT *
      FROM hub_dev.g_external.v_cognite_mdkp_everest
      WHERE coalesce(
              try_to_timestamp(DATETIMESTAMP, 'yyyy-MM-dd HH:mm:ss.SSSSSS'),
              try_to_timestamp(DATETIMESTAMP, 'yyyy-MM-dd HH:mm:ss')
            ) >= current_timestamp() - INTERVAL 1 YEAR
    destination:
      type: "raw"
      database: "db_databricks_everest_raw"
      table: "tb_MDKP"
    primary-key: "{DTNUM}"

# MDTB - MRP Table
  - name: "extract-everest-MDTB"
    database: "db-databricks-raw"
    query: >
      SELECT CONCAT(DTNUM, '_', DTPOS) AS MDTB_PK, *
      FROM hub_dev.g_external.v_cognite_mdtb_everest
      WHERE coalesce(
              try_to_timestamp(DATETIMESTAMP, 'yyyy-MM-dd HH:mm:ss.SSSSSS'),
              try_to_timestamp(DATETIMESTAMP, 'yyyy-MM-dd HH:mm:ss')
            ) >= current_timestamp() - INTERVAL 1 YEAR
    destination:
      type: "raw"
      database: "db_databricks_everest_raw"
      table: "tb_MDTB"
    primary-key: "{MDTB_PK}"

# MDMA - MRP Area for Material
  - name: "extract-everest-MDMA"
    database: "db-databricks-raw"
    query: >
      SELECT CONCAT(MATNR, '_', BERID) AS MDMA_PK, *
      FROM hub_dev.g_external.v_cognite_mdma_everest
      WHERE coalesce(
              try_to_timestamp(DATETIMESTAMP, 'yyyy-MM-dd HH:mm:ss.SSSSSS'),
              try_to_timestamp(DATETIMESTAMP, 'yyyy-MM-dd HH:mm:ss')
            ) >= current_timestamp() - INTERVAL 1 YEAR
    destination:
      type: "raw"
      database: "db_databricks_everest_raw"
      table: "tb_MDMA"
    primary-key: "{MDMA_PK}"

# END MRP

# RESERVATION TABLES

# RKPF - Reservation Document Header
  - name: "extract-everest-RKPF"
    database: "db-databricks-raw"
    query: >
      SELECT *
      FROM hub_dev.g_external.v_cognite_rkpf_everest
      WHERE coalesce(
              try_to_timestamp(DATETIMESTAMP, 'yyyy-MM-dd HH:mm:ss.SSSSSS'),
              try_to_timestamp(DATETIMESTAMP, 'yyyy-MM-dd HH:mm:ss')
            ) >= current_timestamp() - INTERVAL 1 YEAR
    destination:
      type: "raw"
      database: "db_databricks_everest_raw"
      table: "tb_RKPF"
    primary-key: "{RSNUM}"

# RESB - Reservation and Dependent Requirements
  - name: "extract-everest-RESB"
    database: "db-databricks-raw"
    query: >
      SELECT CONCAT(RSNUM, '_', RSPOS, '_', RSART) AS RESB_PK, *
      FROM hub_dev.g_external.v_cognite_resb_everest
      WHERE coalesce(
              try_to_timestamp(DATETIMESTAMP, 'yyyy-MM-dd HH:mm:ss.SSSSSS'),
              try_to_timestamp(DATETIMESTAMP, 'yyyy-MM-dd HH:mm:ss')
            ) >= current_timestamp() - INTERVAL 1 YEAR
    destination:
      type: "raw"
      database: "db_databricks_everest_raw"
      table: "tb_RESB"
    primary-key: "{RESB_PK}"

# END RESERVATION

# PURCHASING TABLES

# EBAN - Purchase Requisition
  - name: "extract-everest-EBAN"
    database: "db-databricks-raw"
    query: >
      SELECT CONCAT(BANFN, '_', BNFPO) AS EBAN_PK, *
      FROM hub_dev.g_external.v_cognite_eban_everest
      WHERE coalesce(
              try_to_timestamp(DATETIMESTAMP, 'yyyy-MM-dd HH:mm:ss.SSSSSS'),
              try_to_timestamp(DATETIMESTAMP, 'yyyy-MM-dd HH:mm:ss')
            ) >= current_timestamp() - INTERVAL 1 YEAR
    destination:
      type: "raw"
      database: "db_databricks_everest_raw"
      table: "tb_EBAN"
    primary-key: "{EBAN_PK}"

# EKKO - Purchasing Document Header
  - name: "extract-everest-EKKO"
    database: "db-databricks-raw"
    query: >
      SELECT *
      FROM hub_dev.g_external.v_cognite_ekko_everest
      WHERE coalesce(
              try_to_timestamp(DATETIMESTAMP, 'yyyy-MM-dd HH:mm:ss.SSSSSS'),
              try_to_timestamp(DATETIMESTAMP, 'yyyy-MM-dd HH:mm:ss')
            ) >= current_timestamp() - INTERVAL 1 YEAR
    destination:
      type: "raw"
      database: "db_databricks_everest_raw"
      table: "tb_EKKO"
    primary-key: "{EBELN}"

# EKPO - Purchasing Document Item
  - name: "extract-everest-EKPO"
    database: "db-databricks-raw"
    query: >
      SELECT CONCAT(EBELN, '_', EBELP) AS EKPO_PK, *
      FROM hub_dev.g_external.v_cognite_ekpo_everest
      WHERE coalesce(
              try_to_timestamp(DATETIMESTAMP, 'yyyy-MM-dd HH:mm:ss.SSSSSS'),
              try_to_timestamp(DATETIMESTAMP, 'yyyy-MM-dd HH:mm:ss')
            ) >= current_timestamp() - INTERVAL 1 YEAR
    destination:
      type: "raw"
      database: "db_databricks_everest_raw"
      table: "tb_EKPO"
    primary-key: "{EKPO_PK}"

# EKKN - Account Assignment in Purchasing Document
  - name: "extract-everest-EKKN"
    database: "db-databricks-raw"
    query: >
      SELECT CONCAT(EBELN, '_', EBELP, '_', ZEKKN) AS EKKN_PK, *
      FROM hub_dev.g_external.v_cognite_ekkn_everest
      WHERE coalesce(
              try_to_timestamp(DATETIMESTAMP, 'yyyy-MM-dd HH:mm:ss.SSSSSS'),
              try_to_timestamp(DATETIMESTAMP, 'yyyy-MM-dd HH:mm:ss')
            ) >= current_timestamp() - INTERVAL 1 YEAR
    destination:
      type: "raw"
      database: "db_databricks_everest_raw"
      table: "tb_EKKN"
    primary-key: "{EKKN_PK}"

# EKET - Scheduling Agreement Schedule Lines
  - name: "extract-everest-EKET"
    database: "db-databricks-raw"
    query: >
      SELECT CONCAT(EBELN, '_', EBELP, '_', ETENR) AS EKET_PK, *
      FROM hub_dev.g_external.v_cognite_eket_everest
      WHERE coalesce(
              try_to_timestamp(DATETIMESTAMP, 'yyyy-MM-dd HH:mm:ss.SSSSSS'),
              try_to_timestamp(DATETIMESTAMP, 'yyyy-MM-dd HH:mm:ss')
            ) >= current_timestamp() - INTERVAL 1 YEAR
    destination:
      type: "raw"
      database: "db_databricks_everest_raw"
      table: "tb_EKET"
    primary-key: "{EKET_PK}"

# EKBE - History per Purchasing Document
  - name: "extract-everest-EKBE"
    database: "db-databricks-raw"
    query: >
      SELECT CONCAT(EBELN, '_', EBELP, '_', ZEKKN, '_', VGABE, '_', GJAHR, '_', BELNR, '_', BUZEI) AS EKBE_PK, *
      FROM hub_dev.g_external.v_cognite_ekbe_everest
      WHERE coalesce(
              try_to_timestamp(DATETIMESTAMP, 'yyyy-MM-dd HH:mm:ss.SSSSSS'),
              try_to_timestamp(DATETIMESTAMP, 'yyyy-MM-dd HH:mm:ss')
            ) >= current_timestamp() - INTERVAL 1 YEAR
    destination:
      type: "raw"
      database: "db_databricks_everest_raw"
      table: "tb_EKBE"
    primary-key: "{EKBE_PK}"

# EINA - Purchasing Info Record General Data
  - name: "extract-everest-EINA"
    database: "db-databricks-raw"
    query: >
      SELECT *
      FROM hub_dev.g_external.v_cognite_eina_everest
      WHERE coalesce(
              try_to_timestamp(DATETIMESTAMP, 'yyyy-MM-dd HH:mm:ss.SSSSSS'),
              try_to_timestamp(DATETIMESTAMP, 'yyyy-MM-dd HH:mm:ss')
            ) >= current_timestamp() - INTERVAL 1 YEAR
    destination:
      type: "raw"
      database: "db_databricks_everest_raw"
      table: "tb_EINA"
    primary-key: "{INFNR}"

# EINE - Purchasing Info Record Purchasing Organization Data
  - name: "extract-everest-EINE"
    database: "db-databricks-raw"
    query: >
      SELECT CONCAT(INFNR, '_', EKORG, '_', ESOKZ, '_', WERKS) AS EINE_PK, *
      FROM hub_dev.g_external.v_cognite_eine_everest
      WHERE coalesce(
              try_to_timestamp(DATETIMESTAMP, 'yyyy-MM-dd HH:mm:ss.SSSSSS'),
              try_to_timestamp(DATETIMESTAMP, 'yyyy-MM-dd HH:mm:ss')
            ) >= current_timestamp() - INTERVAL 1 YEAR
    destination:
      type: "raw"
      database: "db_databricks_everest_raw"
      table: "tb_EINE"
    primary-key: "{EINE_PK}"

# LFA1 - Vendor Master General Section
  - name: "extract-everest-LFA1"
    database: "db-databricks-raw"
    query: >
      SELECT *
      FROM hub_dev.g_external.v_cognite_lfa1_everest
      WHERE coalesce(
              try_to_timestamp(DATETIMESTAMP, 'yyyy-MM-dd HH:mm:ss.SSSSSS'),
              try_to_timestamp(DATETIMESTAMP, 'yyyy-MM-dd HH:mm:ss')
            ) >= current_timestamp() - INTERVAL 1 YEAR
    destination:
      type: "raw"
      database: "db_databricks_everest_raw"
      table: "tb_LFA1"
    primary-key: "{LIFNR}"

# END PURCHASING

# SALES & DELIVERY TABLES

# KNA1 - General Data in Customer Master
  - name: "extract-everest-KNA1"
    database: "db-databricks-raw"
    query: >
      SELECT *
      FROM hub_dev.g_external.v_cognite_kna1_everest
      WHERE coalesce(
              try_to_timestamp(DATETIMESTAMP, 'yyyy-MM-dd HH:mm:ss.SSSSSS'),
              try_to_timestamp(DATETIMESTAMP, 'yyyy-MM-dd HH:mm:ss')
            ) >= current_timestamp() - INTERVAL 1 YEAR
    destination:
      type: "raw"
      database: "db_databricks_everest_raw"
      table: "tb_KNA1"
    primary-key: "{KUNNR}"

# KNVV - Customer Master Sales Data
  - name: "extract-everest-KNVV"
    database: "db-databricks-raw"
    query: >
      SELECT CONCAT(KUNNR, '_', VKORG, '_', VTWEG, '_', SPART) AS KNVV_PK, *
      FROM hub_dev.g_external.v_cognite_knvv_everest
      WHERE coalesce(
              try_to_timestamp(DATETIMESTAMP, 'yyyy-MM-dd HH:mm:ss.SSSSSS'),
              try_to_timestamp(DATETIMESTAMP, 'yyyy-MM-dd HH:mm:ss')
            ) >= current_timestamp() - INTERVAL 1 YEAR
    destination:
      type: "raw"
      database: "db_databricks_everest_raw"
      table: "tb_KNVV"
    primary-key: "{KNVV_PK}"

# KNVK - Customer Master Contact Partner
  - name: "extract-everest-KNVK"
    database: "db-databricks-raw"
    query: >
      SELECT *
      FROM hub_dev.g_external.v_cognite_knvk_everest
      WHERE coalesce(
              try_to_timestamp(DATETIMESTAMP, 'yyyy-MM-dd HH:mm:ss.SSSSSS'),
              try_to_timestamp(DATETIMESTAMP, 'yyyy-MM-dd HH:mm:ss')
            ) >= current_timestamp() - INTERVAL 1 YEAR
    destination:
      type: "raw"
      database: "db_databricks_everest_raw"
      table: "tb_KNVK"
    primary-key: "{PARNR}"

# LIKP - Delivery Header Data
  - name: "extract-everest-LIKP"
    database: "db-databricks-raw"
    query: >
      SELECT *
      FROM hub_dev.g_external.v_cognite_likp_everest
      WHERE coalesce(
              try_to_timestamp(DATETIMESTAMP, 'yyyy-MM-dd HH:mm:ss.SSSSSS'),
              try_to_timestamp(DATETIMESTAMP, 'yyyy-MM-dd HH:mm:ss')
            ) >= current_timestamp() - INTERVAL 1 YEAR
    destination:
      type: "raw"
      database: "db_databricks_everest_raw"
      table: "tb_LIKP"
    primary-key: "{VBELN}"

# LIPS - Delivery Item Data
  - name: "extract-everest-LIPS"
    database: "db-databricks-raw"
    query: >
      SELECT CONCAT(VBELN, '_', POSNR) AS LIPS_PK, *
      FROM hub_dev.g_external.v_cognite_lips_everest
      WHERE coalesce(
              try_to_timestamp(DATETIMESTAMP, 'yyyy-MM-dd HH:mm:ss.SSSSSS'),
              try_to_timestamp(DATETIMESTAMP, 'yyyy-MM-dd HH:mm:ss')
            ) >= current_timestamp() - INTERVAL 1 YEAR
    destination:
      type: "raw"
      database: "db_databricks_everest_raw"
      table: "tb_LIPS"
    primary-key: "{LIPS_PK}"

# END SALES & DELIVERY

# QUALITY MANAGEMENT TABLES

# QMEL - Quality Notification
  - name: "extract-everest-QMEL"
    database: "db-databricks-raw"
    query: >
      SELECT *
      FROM hub_dev.g_external.v_cognite_qmel_everest
      WHERE coalesce(
              try_to_timestamp(DATETIMESTAMP, 'yyyy-MM-dd HH:mm:ss.SSSSSS'),
              try_to_timestamp(DATETIMESTAMP, 'yyyy-MM-dd HH:mm:ss')
            ) >= current_timestamp() - INTERVAL 1 YEAR
    destination:
      type: "raw"
      database: "db_databricks_everest_raw"
      table: "tb_QMEL"
    primary-key: "{QMNUM}"

# QMFE - Quality Notification Items
  - name: "extract-everest-QMFE"
    database: "db-databricks-raw"
    query: >
      SELECT CONCAT(QMNUM, '_', FENUM) AS QMFE_PK, *
      FROM hub_dev.g_external.v_cognite_qmfe_everest
      WHERE coalesce(
              try_to_timestamp(DATETIMESTAMP, 'yyyy-MM-dd HH:mm:ss.SSSSSS'),
              try_to_timestamp(DATETIMESTAMP, 'yyyy-MM-dd HH:mm:ss')
            ) >= current_timestamp() - INTERVAL 1 YEAR
    destination:
      type: "raw"
      database: "db_databricks_everest_raw"
      table: "tb_QMFE"
    primary-key: "{QMFE_PK}"

# QMIH - Quality Notification Maintenance Data
  - name: "extract-everest-QMIH"
    database: "db-databricks-raw"
    query: >
      SELECT *
      FROM hub_dev.g_external.v_cognite_qmih_everest
      WHERE coalesce(
              try_to_timestamp(DATETIMESTAMP, 'yyyy-MM-dd HH:mm:ss.SSSSSS'),
              try_to_timestamp(DATETIMESTAMP, 'yyyy-MM-dd HH:mm:ss')
            ) >= current_timestamp() - INTERVAL 1 YEAR
    destination:
      type: "raw"
      database: "db_databricks_everest_raw"
      table: "tb_QMIH"
    primary-key: "{QMNUM}"

# QMSM - Quality Notification Tasks
  - name: "extract-everest-QMSM"
    database: "db-databricks-raw"
    query: >
      SELECT CONCAT(QMNUM, '_', MANUM) AS QMSM_PK, *
      FROM hub_dev.g_external.v_cognite_qmsm_everest
      WHERE coalesce(
              try_to_timestamp(DATETIMESTAMP, 'yyyy-MM-dd HH:mm:ss.SSSSSS'),
              try_to_timestamp(DATETIMESTAMP, 'yyyy-MM-dd HH:mm:ss')
            ) >= current_timestamp() - INTERVAL 1 YEAR
    destination:
      type: "raw"
      database: "db_databricks_everest_raw"
      table: "tb_QMSM"
    primary-key: "{QMSM_PK}"

# QMUR - Quality Notification Causes
  - name: "extract-everest-QMUR"
    database: "db-databricks-raw"
    query: >
      SELECT CONCAT(QMNUM, '_', FENUM, '_', URNUM) AS QMUR_PK, *
      FROM hub_dev.g_external.v_cognite_qmur_everest
      WHERE coalesce(
              try_to_timestamp(DATETIMESTAMP, 'yyyy-MM-dd HH:mm:ss.SSSSSS'),
              try_to_timestamp(DATETIMESTAMP, 'yyyy-MM-dd HH:mm:ss')
            ) >= current_timestamp() - INTERVAL 1 YEAR
    destination:
      type: "raw"
      database: "db_databricks_everest_raw"
      table: "tb_QMUR"
    primary-key: "{QMUR_PK}"

# QALS - Inspection Lot Record
  - name: "extract-everest-QALS"
    database: "db-databricks-raw"
    query: >
      SELECT *
      FROM hub_dev.g_external.v_cognite_qals_everest
      WHERE coalesce(
              try_to_timestamp(DATETIMESTAMP, 'yyyy-MM-dd HH:mm:ss.SSSSSS'),
              try_to_timestamp(DATETIMESTAMP, 'yyyy-MM-dd HH:mm:ss')
            ) >= current_timestamp() - INTERVAL 1 YEAR
    destination:
      type: "raw"
      database: "db_databricks_everest_raw"
      table: "tb_QALS"
    primary-key: "{PRUEFLOS}"

# QAMR - Characteristic Results During Inspection
  - name: "extract-everest-QAMR"
    database: "db-databricks-raw"
    query: >
      SELECT CONCAT(PRUEFLOS, '_', VORGLFNR, '_', MERKNR, '_', DETAILERG) AS QAMR_PK, *
      FROM hub_dev.g_external.v_cognite_qamr_everest
      WHERE coalesce(
              try_to_timestamp(DATETIMESTAMP, 'yyyy-MM-dd HH:mm:ss.SSSSSS'),
              try_to_timestamp(DATETIMESTAMP, 'yyyy-MM-dd HH:mm:ss')
            ) >= current_timestamp() - INTERVAL 1 YEAR
    destination:
      type: "raw"
      database: "db_databricks_everest_raw"
      table: "tb_QAMR"
    primary-key: "{QAMR_PK}"

# QAVE - Inspection Usage Decision
  - name: "extract-everest-QAVE"
    database: "db-databricks-raw"
    query: >
      SELECT CONCAT(PRUEFLOS, '_', KZART, '_', ZAEHLER) AS QAVE_PK, *
      FROM hub_dev.g_external.v_cognite_qave_everest
      WHERE coalesce(
              try_to_timestamp(DATETIMESTAMP, 'yyyy-MM-dd HH:mm:ss.SSSSSS'),
              try_to_timestamp(DATETIMESTAMP, 'yyyy-MM-dd HH:mm:ss')
            ) >= current_timestamp() - INTERVAL 1 YEAR
    destination:
      type: "raw"
      database: "db_databricks_everest_raw"
      table: "tb_QAVE"
    primary-key: "{QAVE_PK}"

# QMAT - Inspection Type Material Parameters
  - name: "extract-everest-QMAT"
    database: "db-databricks-raw"
    query: >
      SELECT CONCAT(ART, '_', MATNR, '_', WERKS) AS QMAT_PK, *
      FROM hub_dev.g_external.v_cognite_qmat_everest
      WHERE coalesce(
              try_to_timestamp(DATETIMESTAMP, 'yyyy-MM-dd HH:mm:ss.SSSSSS'),
              try_to_timestamp(DATETIMESTAMP, 'yyyy-MM-dd HH:mm:ss')
            ) >= current_timestamp() - INTERVAL 1 YEAR
    destination:
      type: "raw"
      database: "db_databricks_everest_raw"
      table: "tb_QMAT"
    primary-key: "{QMAT_PK}"

# QPGR - Code Groups
  - name: "extract-everest-QPGR"
    database: "db-databricks-raw"
    query: >
      SELECT CONCAT(KATALOGART, '_', CODEGRUPPE) AS QPGR_PK, *
      FROM hub_dev.g_external.v_cognite_qpgr_everest
      WHERE coalesce(
              try_to_timestamp(DATETIMESTAMP, 'yyyy-MM-dd HH:mm:ss.SSSSSS'),
              try_to_timestamp(DATETIMESTAMP, 'yyyy-MM-dd HH:mm:ss')
            ) >= current_timestamp() - INTERVAL 1 YEAR
    destination:
      type: "raw"
      database: "db_databricks_everest_raw"
      table: "tb_QPGR"
    primary-key: "{QPGR_PK}"

# QPGT - Code Group Texts
  - name: "extract-everest-QPGT"
    database: "db-databricks-raw"
    query: >
      SELECT CONCAT(KATALOGART, '_', CODEGRUPPE, '_', SPRACHE) AS QPGT_PK, *
      FROM hub_dev.g_external.v_cognite_qpgt_everest
      WHERE coalesce(
              try_to_timestamp(DATETIMESTAMP, 'yyyy-MM-dd HH:mm:ss.SSSSSS'),
              try_to_timestamp(DATETIMESTAMP, 'yyyy-MM-dd HH:mm:ss')
            ) >= current_timestamp() - INTERVAL 1 YEAR
    destination:
      type: "raw"
      database: "db_databricks_everest_raw"
      table: "tb_QPGT"
    primary-key: "{QPGT_PK}"

# QPCT - Code Texts
  - name: "extract-everest-QPCT"
    database: "db-databricks-raw"
    query: >
      SELECT CONCAT(KATALOGART, '_', CODEGRUPPE, '_', CODE, '_', SPRACHE, '_', VERSION) AS QPCT_PK, *
      FROM hub_dev.g_external.v_cognite_qpct_everest
      WHERE coalesce(
              try_to_timestamp(DATETIMESTAMP, 'yyyy-MM-dd HH:mm:ss.SSSSSS'),
              try_to_timestamp(DATETIMESTAMP, 'yyyy-MM-dd HH:mm:ss')
            ) >= current_timestamp() - INTERVAL 1 YEAR
    destination:
      type: "raw"
      database: "db_databricks_everest_raw"
      table: "tb_QPCT"
    primary-key: "{QPCT_PK}"

# END QUALITY MANAGEMENT

# SERIAL NUMBER TABLES

# OBJK - Plant Maintenance Object List
  - name: "extract-everest-OBJK"
    database: "db-databricks-raw"
    query: >
      SELECT CONCAT(OBKNR, '_', OBZAE) AS OBJK_PK, *
      FROM hub_dev.g_external.v_cognite_objk_everest
      WHERE coalesce(
              try_to_timestamp(DATETIMESTAMP, 'yyyy-MM-dd HH:mm:ss.SSSSSS'),
              try_to_timestamp(DATETIMESTAMP, 'yyyy-MM-dd HH:mm:ss')
            ) >= current_timestamp() - INTERVAL 1 YEAR
    destination:
      type: "raw"
      database: "db_databricks_everest_raw"
      table: "tb_OBJK"
    primary-key: "{OBJK_PK}"

# SER01 - Serial Numbers for Delivery
  - name: "extract-everest-SER01"
    database: "db-databricks-raw"
    query: >
      SELECT *
      FROM hub_dev.g_external.v_cognite_ser01_everest
      WHERE coalesce(
              try_to_timestamp(DATETIMESTAMP, 'yyyy-MM-dd HH:mm:ss.SSSSSS'),
              try_to_timestamp(DATETIMESTAMP, 'yyyy-MM-dd HH:mm:ss')
            ) >= current_timestamp() - INTERVAL 1 YEAR
    destination:
      type: "raw"
      database: "db_databricks_everest_raw"
      table: "tb_SER01"
    primary-key: "{OBKNR}"

# SER03 - Serial Numbers for Goods Movements
  - name: "extract-everest-SER03"
    database: "db-databricks-raw"
    query: >
      SELECT *
      FROM hub_dev.g_external.v_cognite_ser03_everest
      WHERE coalesce(
              try_to_timestamp(DATETIMESTAMP, 'yyyy-MM-dd HH:mm:ss.SSSSSS'),
              try_to_timestamp(DATETIMESTAMP, 'yyyy-MM-dd HH:mm:ss')
            ) >= current_timestamp() - INTERVAL 1 YEAR
    destination:
      type: "raw"
      database: "db_databricks_everest_raw"
      table: "tb_SER03"
    primary-key: "{OBKNR}"

# SER05 - Serial Numbers for PP Order
  - name: "extract-everest-SER05"
    database: "db-databricks-raw"
    query: >
      SELECT *
      FROM hub_dev.g_external.v_cognite_ser05_everest
      WHERE coalesce(
              try_to_timestamp(DATETIMESTAMP, 'yyyy-MM-dd HH:mm:ss.SSSSSS'),
              try_to_timestamp(DATETIMESTAMP, 'yyyy-MM-dd HH:mm:ss')
            ) >= current_timestamp() - INTERVAL 1 YEAR
    destination:
      type: "raw"
      database: "db_databricks_everest_raw"
      table: "tb_SER05"
    primary-key: "{OBKNR}"

# END SERIAL NUMBER

# ORGANIZATIONAL & CUSTOMIZING TABLES

# T001 - Company Codes
  - name: "extract-everest-T001"
    database: "db-databricks-raw"
    query: >
      SELECT *
      FROM hub_dev.g_external.v_cognite_t001_everest
      WHERE coalesce(
              try_to_timestamp(DATETIMESTAMP, 'yyyy-MM-dd HH:mm:ss.SSSSSS'),
              try_to_timestamp(DATETIMESTAMP, 'yyyy-MM-dd HH:mm:ss')
            ) >= current_timestamp() - INTERVAL 1 YEAR
    destination:
      type: "raw"
      database: "db_databricks_everest_raw"
      table: "tb_T001"
    primary-key: "{BUKRS}"

# T001K - Valuation Area
  - name: "extract-everest-T001K"
    database: "db-databricks-raw"
    query: >
      SELECT *
      FROM hub_dev.g_external.v_cognite_t001k_everest
      WHERE coalesce(
              try_to_timestamp(DATETIMESTAMP, 'yyyy-MM-dd HH:mm:ss.SSSSSS'),
              try_to_timestamp(DATETIMESTAMP, 'yyyy-MM-dd HH:mm:ss')
            ) >= current_timestamp() - INTERVAL 1 YEAR
    destination:
      type: "raw"
      database: "db_databricks_everest_raw"
      table: "tb_T001K"
    primary-key: "{BWKEY}"

# T001L - Storage Locations
  - name: "extract-everest-T001L"
    database: "db-databricks-raw"
    query: >
      SELECT CONCAT(WERKS, '_', LGORT) AS T001L_PK, *
      FROM hub_dev.g_external.v_cognite_t001l_everest
      WHERE coalesce(
              try_to_timestamp(DATETIMESTAMP, 'yyyy-MM-dd HH:mm:ss.SSSSSS'),
              try_to_timestamp(DATETIMESTAMP, 'yyyy-MM-dd HH:mm:ss')
            ) >= current_timestamp() - INTERVAL 1 YEAR
    destination:
      type: "raw"
      database: "db_databricks_everest_raw"
      table: "tb_T001L"
    primary-key: "{T001L_PK}"

# T001W - Plants and Branches
  - name: "extract-everest-T001W"
    database: "db-databricks-raw"
    query: >
      SELECT *
      FROM hub_dev.g_external.v_cognite_t001w_everest
      WHERE coalesce(
              try_to_timestamp(DATETIMESTAMP, 'yyyy-MM-dd HH:mm:ss.SSSSSS'),
              try_to_timestamp(DATETIMESTAMP, 'yyyy-MM-dd HH:mm:ss')
            ) >= current_timestamp() - INTERVAL 1 YEAR
    destination:
      type: "raw"
      database: "db_databricks_everest_raw"
      table: "tb_T001W"
    primary-key: "{WERKS}"

# T003P - Order Type Descriptions
  - name: "extract-everest-T003P"
    database: "db-databricks-raw"
    query: >
      SELECT CONCAT(SPRAS, '_', AUART) AS T003P_PK, *
      FROM hub_dev.g_external.v_cognite_t003p_everest
      WHERE coalesce(
              try_to_timestamp(DATETIMESTAMP, 'yyyy-MM-dd HH:mm:ss.SSSSSS'),
              try_to_timestamp(DATETIMESTAMP, 'yyyy-MM-dd HH:mm:ss')
            ) >= current_timestamp() - INTERVAL 1 YEAR
    destination:
      type: "raw"
      database: "db_databricks_everest_raw"
      table: "tb_T003P"
    primary-key: "{T003P_PK}"

# T006 - Units of Measurement
  - name: "extract-everest-T006"
    database: "db-databricks-raw"
    query: >
      SELECT *
      FROM hub_dev.g_external.v_cognite_t006_everest
      WHERE coalesce(
              try_to_timestamp(DATETIMESTAMP, 'yyyy-MM-dd HH:mm:ss.SSSSSS'),
              try_to_timestamp(DATETIMESTAMP, 'yyyy-MM-dd HH:mm:ss')
            ) >= current_timestamp() - INTERVAL 1 YEAR
    destination:
      type: "raw"
      database: "db_databricks_everest_raw"
      table: "tb_T006"
    primary-key: "{MSEHI}"

# T006A - Language-Dependent Unit of Measure
  - name: "extract-everest-T006A"
    database: "db-databricks-raw"
    query: >
      SELECT CONCAT(SPRAS, '_', MSEHI) AS T006A_PK, *
      FROM hub_dev.g_external.v_cognite_t006a_everest
      WHERE coalesce(
              try_to_timestamp(DATETIMESTAMP, 'yyyy-MM-dd HH:mm:ss.SSSSSS'),
              try_to_timestamp(DATETIMESTAMP, 'yyyy-MM-dd HH:mm:ss')
            ) >= current_timestamp() - INTERVAL 1 YEAR
    destination:
      type: "raw"
      database: "db_databricks_everest_raw"
      table: "tb_T006A"
    primary-key: "{T006A_PK}"

# T023 - Material Groups
  - name: "extract-everest-T023"
    database: "db-databricks-raw"
    query: >
      SELECT *
      FROM hub_dev.g_external.v_cognite_t023_everest
      WHERE coalesce(
              try_to_timestamp(DATETIMESTAMP, 'yyyy-MM-dd HH:mm:ss.SSSSSS'),
              try_to_timestamp(DATETIMESTAMP, 'yyyy-MM-dd HH:mm:ss')
            ) >= current_timestamp() - INTERVAL 1 YEAR
    destination:
      type: "raw"
      database: "db_databricks_everest_raw"
      table: "tb_T023"
    primary-key: "{MATKL}"

# T023T - Material Group Descriptions
  - name: "extract-everest-T023T"
    database: "db-databricks-raw"
    query: >
      SELECT CONCAT(SPRAS, '_', MATKL) AS T023T_PK, *
      FROM hub_dev.g_external.v_cognite_t023t_everest
      WHERE coalesce(
              try_to_timestamp(DATETIMESTAMP, 'yyyy-MM-dd HH:mm:ss.SSSSSS'),
              try_to_timestamp(DATETIMESTAMP, 'yyyy-MM-dd HH:mm:ss')
            ) >= current_timestamp() - INTERVAL 1 YEAR
    destination:
      type: "raw"
      database: "db_databricks_everest_raw"
      table: "tb_T023T"
    primary-key: "{T023T_PK}"

# T024 - Purchasing Groups
  - name: "extract-everest-T024"
    database: "db-databricks-raw"
    query: >
      SELECT *
      FROM hub_dev.g_external.v_cognite_t024_everest
      WHERE coalesce(
              try_to_timestamp(DATETIMESTAMP, 'yyyy-MM-dd HH:mm:ss.SSSSSS'),
              try_to_timestamp(DATETIMESTAMP, 'yyyy-MM-dd HH:mm:ss')
            ) >= current_timestamp() - INTERVAL 1 YEAR
    destination:
      type: "raw"
      database: "db_databricks_everest_raw"
      table: "tb_T024"
    primary-key: "{EKGRP}"

# END ORGANIZATIONAL & CUSTOMIZING
